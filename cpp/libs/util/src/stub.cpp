#include "adl2/util/util.hpp"

// P1 placeholder so the static library is non-empty and the CMake target
// participates in the dependency spine. No core logic lives here yet.
namespace adl2::util {
int module_anchor() { return 0; }
}  // namespace adl2::util
