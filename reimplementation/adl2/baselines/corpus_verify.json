{
  "schema": "smash2-corpus-verify/1",
  "source": "remeasured after the PRESENCE MODEL (Phase B, SPEC_PRESENCE_MODEL.md), which closes the absent-property seam: every cut over a possibly-absent quantity encodes `defined(q) >= 1 AND atom`, negation becomes faithful without a hedge, the Phase-A `guarded_not` machinery is deleted, and every element-touching axiom family is emitted through a presence guard. PROVEN DISJOINT 794 -> 813. The +19 is fully accounted for: +18 is EXACTLY the set withdrawn on 2026-07-25 when the Phase-A hedge landed (all in the CMS-SUS-16-041 variants, all certified), and +1 is the new golden presence_04, whose regions are disjoint on definedness alone. files/pairs are +6 for the six new golden/presence_*.adl pins. PROVEN OVERLAPPING 72 -> 76 (+4 from new goldens; the corpus set is unchanged). Subset claims 219 -> 248: +29 recovered inners, because a `reject` no longer poisons a region's under-projection (47 corpus regions regained `exact`), and -1 WITHDRAWN, which was a false claim (golden features-num_09 shipped `subset R_plain_met within R_cancel_met` while `MET + HT - HT > 50` hard-errors on an event with no HT scalar; verified with `smash2 run`). Sampling and refute refutations are 0 corpus-wide and every proven-disjoint pair is certified (813/813).",
  "files": 145,
  "files_exit_0": 145,
  "pairs": 1900,
  "proven_disjoint": 813,
  "proven_overlapping": 76,
  "candidate_overlapping": 45,
  "possibly": 966,
  "unknown": 0
}
