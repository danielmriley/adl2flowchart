{
  "schema": "smash2-corpus-verify/1",
  "source": "remeasured after the PRESENCE MODEL (Phase B, SPEC_PRESENCE_MODEL.md), which closes the absent-property seam: every cut over a possibly-absent quantity encodes `defined(q) >= 1 AND atom`, negation becomes faithful without a hedge, the Phase-A `guarded_not` machinery is deleted, and every element-touching axiom family is emitted through a presence guard. PROVEN DISJOINT 794 -> 813. The +19 is fully accounted for: +18 is EXACTLY the set withdrawn on 2026-07-25 when the Phase-A hedge landed (all in the CMS-SUS-16-041 variants, all certified), and +1 is the new golden presence_04, whose regions are disjoint on definedness alone. files are +7 for the seven new golden/presence_*.adl pins (one of them, presence_07, pins a BIN COVERAGE gap rather than a pair, so it adds no pair). PROVEN OVERLAPPING 72 -> 76 (+4 from new goldens; the corpus set is unchanged). Subset claims 219 -> 248: +29 recovered inners, because a `reject` no longer poisons a region's under-projection (47 corpus regions regained `exact`), and -1 WITHDRAWN, which was a false claim (golden features-num_09 shipped `subset R_plain_met within R_cancel_met` while `MET + HT - HT > 50` hard-errors on an event with no HT scalar; verified with `smash2 run`). Sampling and refute refutations are 0 corpus-wide and every proven-disjoint pair is certified (813/813). REMEASURED 2026-08-01 after K14 (SOUNDNESS_PROOF §8 item 1c, the min-pair separation): every count above is UNCHANGED and the per-pair table differs in exactly two lines — `EWSRs within baseline` and `SPSRs within baseline` in CMS-SUS-21-009_Delphes.adl are WITHDRAWN, because `dR(AK4jets[0:1], METLV[0])` has one unindexed leg, which `Interp::quantity` hard-errors on, so the interpreter decides nothing for any region reading it and the encoder now refuses it too. Both withdrawn claims were true by inheritance and vacuous in fact; they are now reported as an explicit ENCODING GAP naming the line. Subset claims 248 -> 246; PROVEN DISJOINT still 813, still 813/813 certified.",
  "files": 146,
  "files_exit_0": 146,
  "pairs": 1900,
  "proven_disjoint": 813,
  "proven_overlapping": 76,
  "candidate_overlapping": 45,
  "possibly": 966,
  "unknown": 0
}
